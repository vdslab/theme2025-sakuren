import { AppBar } from "@mui/material";

export const Header = () => {
  return (
    <AppBar position="fixed">
      <h1 style={{ textAlign: "center", padding: "8px 0" }}>Word Cloud Map</h1>
    </AppBar>
  );
};
